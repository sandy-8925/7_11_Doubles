package org.provinggrounds.SevenElevenDoubles.Game;

/**
 * Used to hold game properties during command input stage.
 * When game is ready to be started, can be asked to construct game object.
 */
public final class GameFactory {

    /**
     * Informs caller whether game start has been signalled.
     * @return True if game can be started, and 'start' command has been issued. False otherwise.
     */
    public static boolean gameStartSignalled() {
        //stub
        return true;
    }
}
